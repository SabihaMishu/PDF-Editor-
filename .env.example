# Server Configuration
PROJECT_NAME="PDF Editor API"
VERSION="1.0.0"
API_PREFIX="/api"

# File limits (in bytes, 25MB = 26214400)
MAX_FILE_SIZE_BYTES=26214400

# CORS settings (comma-separated origins or ["*"])
CORS_ORIGINS=["*"]
